# Agent Secrets — rename this file to secrets.env and fill in your values.
# secrets.env is gitignored and NEVER committed to source control.
#
# These secrets are available to tools at execution time via context.agentSecrets.
# The LLM never sees these values — they stay server-side in the tool layer.
#
# ─── Instance API ───────────────────────────────────────────────────────────
# INSTANCE_URL=https://api.customer.example.com
# INSTANCE_API_TOKEN=Bearer eyJhbGciOiJIUz...
#
# ─── Azure DevOps ──────────────────────────────────────────────────────────
# AZDO_PAT=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AZDO_ORG=yourorg
# AZDO_PROJECT=YourProject
#
# ─── Git / GitHub ──────────────────────────────────────────────────────────
# GIT_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GIT_REMOTE=https://github.com/org/repo.git
#
# ─── Slack (if agent needs its own bot, otherwise uses system-level) ───────
# SLACK_BOT_TOKEN=xoxb-...
#
# ─── Custom keys ───────────────────────────────────────────────────────────
# Any KEY=VALUE pair here becomes available as context.agentSecrets.KEY
# in your agent's tools.
